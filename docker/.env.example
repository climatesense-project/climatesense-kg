# Copy this file to .env and update the values

# Select exactly one triplestore profile (virtuoso, qlever)
COMPOSE_PROFILES=qlever

# GitHub token for accessing private repositories
GITHUB_TOKEN=your_github_token_here

# Virtuoso configuration
VIRTUOSO_HOST=virtuoso
VIRTUOSO_BIND_ADDRESS=127.0.0.1
VIRTUOSO_PORT=8890
VIRTUOSO_USER=dba
VIRTUOSO_PASSWORD=

# QLever configuration
# Internal SPARQL endpoint used by analytics services
QLEVER_ENDPOINT=http://qlever:7019
QLEVER_PORT=7019
QLEVER_ACCESS_TOKEN=replace_with_a_long_random_token
QLEVER_INDEX_MEMORY=1G
QLEVER_MEMORY_FOR_QUERIES=5G
QLEVER_CACHE_MAX_SIZE=2G
QLEVER_QUERY_TIMEOUT=30s
# UID/GID that own the persistent QLever index volume
QLEVER_UID=999
QLEVER_GID=999

# QLever UI configuration
QLEVER_UI_PORT=7018
QLEVERUI_SECRET_KEY=replace_with_a_long_random_secret
QLEVERUI_DEBUG=False
QLEVERUI_ALLOWED_HOSTS=localhost,127.0.0.1
QLEVERUI_CSRF_TRUSTED_ORIGINS=http://localhost:7018
QLEVER_UI_BACKEND_URL=http://localhost:7018/qlever

# CIMPLE Factors API configuration
# This URL must be reachable from the pipeline container (not container-local localhost)
CIMPLE_FACTORS_API_URL=

# UID/GID used by the pipeline container for bind-mounted output files
PIPELINE_UID=1000
PIPELINE_GID=1000

# Durable PostgreSQL pipeline-state configuration
POSTGRES_HOST=postgres
POSTGRES_BIND_ADDRESS=127.0.0.1
POSTGRES_HOST_PORT=5432
POSTGRES_DB=climatesense
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# Analytics API service configuration
# The analytics API falls back to the POSTGRES_* values when this DSN is omitted
# ANALYTICS_DATABASE_DSN=postgresql+asyncpg://postgres:postgres@postgres:5432/climatesense
# Defaults to QLEVER_ENDPOINT
# For Virtuoso, use: ANALYTICS_SPARQL_ENDPOINT=http://virtuoso:8890/sparql
ANALYTICS_SPARQL_ENDPOINT=http://qlever:7019
# ANALYTICS_SPARQL_USER=
# ANALYTICS_SPARQL_PASSWORD=
ANALYTICS_ALLOWED_ORIGINS=http://localhost:3000
ANALYTICS_RESULT_CACHE_TTL=300
ANALYTICS_SPARQL_TIMEOUT=20
ANALYTICS_API_PORT=8000

# Analytics UI configuration
NEXT_PUBLIC_ANALYTICS_API_URL=http://localhost:8000
ANALYTICS_UI_PORT=3000

# Virtuoso performance tuning parameters
# (https://docs.openlinksw.com/virtuoso/rdfperformancetuning/#rdfperfgeneral)
VIRT_PARAMETERS_NUMBEROFBUFFERS=340000
VIRT_PARAMETERS_MAXDIRTYBUFFERS=250000
VIRT_PARAMETERS_CHECKPOINTINTERVAL=60
VIRT_PARAMETERS_FREETEXTBATCHSIZE=100000
VIRT_PARAMETERS_SERVERTHREADS=20
VIRT_DATABASE_MAXCHECKPOINTREMAP=16000
VIRT_TEMPDATABASE_MAXCHECKPOINTREMAP=16000
VIRT_HTTPSERVER_SERVERTHREADS=10
VIRT_HTTPSERVER_HTTPTHREADSIZE=280000
# Directories Virtuoso may read for bulk loads (ld_dir). /database/data is where
# RDF snapshots and catalogs are mounted, so it must stay in this list.
# Takes effect only when the virtuoso_data volume is first created.
VIRT_PARAMETERS_DIRSALLOWED=., ../vad, /usr/share/proj, /database/data
